/**
 * ANGULAR 2 LOCALIZATION
 * An Angular 2 library to translate messages, dates and numbers.
 * Written by Roberto Simonetti.
 * MIT license.
 * https://github.com/robisim74/angular2localization
 */
import { ElementRef, Renderer, AfterViewInit } from '@angular/core';
import { LocaleService } from '../services/locale.service';
/**
 * I18nPlural superclass.
 *
 * @author Roberto Simonetti
 */
export declare class I18nPlural {
    locale: LocaleService;
    protected keyStr: string;
    private readonly REGEXP;
    constructor(locale: LocaleService);
    protected translateNumber(key: string): string;
}
/**
 * LocaleDirective superclass.
 *
 * @author Roberto Simonetti
 */
export declare abstract class LocaleDirective implements AfterViewInit {
    locale: LocaleService;
    protected el: ElementRef;
    protected renderer: Renderer;
    constructor(locale: LocaleService, el: ElementRef, renderer: Renderer);
    ngAfterViewInit(): void;
    protected abstract localize(renderNode: any, value: string): void;
}
