/**
 * ANGULAR 2 LOCALIZATION
 * An Angular 2 library to translate messages, dates and numbers.
 * Written by Roberto Simonetti.
 * MIT license.
 * https://github.com/robisim74/angular2localization
 */
import { ElementRef, Renderer } from '@angular/core';
import { LocaleDirective } from '../services/i18n';
import { LocaleService } from '../services/locale.service';
export declare class LocaleDecimalDirective extends LocaleDirective {
    locale: LocaleService;
    digits: string;
    private defaultDigits;
    constructor(locale: LocaleService, el: ElementRef, renderer: Renderer);
    localize(renderNode: any, value: string): void;
}
export declare class LocalePercentDirective extends LocaleDirective {
    locale: LocaleService;
    digits: string;
    private defaultDigits;
    constructor(locale: LocaleService, el: ElementRef, renderer: Renderer);
    localize(renderNode: any, value: string): void;
}
export declare class LocaleCurrencyDirective extends LocaleDirective {
    locale: LocaleService;
    digits: string;
    symbol: boolean;
    private symbolDisplay;
    private defaultDigits;
    constructor(locale: LocaleService, el: ElementRef, renderer: Renderer);
    localize(renderNode: any, value: string): void;
}
