/**
 * ANGULAR 2 LOCALIZATION
 * An Angular 2 library to translate messages, dates and numbers.
 * Written by Roberto Simonetti.
 * MIT license.
 * https://github.com/robisim74/angular2localization
 */
import { ElementRef, Renderer, AfterViewInit } from '@angular/core';
import { LocalizationService } from '../services/localization.service';
export declare class TranslateDirective implements AfterViewInit {
    localization: LocalizationService;
    private el;
    private renderer;
    params: string;
    constructor(localization: LocalizationService, el: ElementRef, renderer: Renderer);
    ngAfterViewInit(): void;
    private translate(renderNode, key);
}
