import { GroupDescriptor, GroupResult } from './group-descriptor.interface';
import { Reducer } from '../common.interfaces';
/**
 * @hidden
 */
export declare const normalizeGroups: (descriptors: any) => any;
/**
 * Groups the provided data according to the specified descriptors.
 *
 * @param {Array} data
 * @param {GroupDescriptor[]} [descriptors=[]]
 * @param {any} [initialTransformers=identity]
 * @param {any} [transformers=identity]
 * @param {Array} [originalData=data]
 * @returns {(Array<GroupResult<T>> | T[])}
 *
 * @example
 * ```ts-no-run
 *
 * import { groupBy } from '@progress/kendo-data-query';
 *
 * const data = [
 *     { name: "Pork", category: "Food", subcategory: "Meat" },
 *     { name: "Pepper", category: "Food", subcategory: "Vegetables" },
 *     { name: "Beef", category: "Food", subcategory: "Meat" }
 * ];
 *
 * const result = groupBy(data, [{ field: "subcategory" }]);
 * ```
 */
export declare const groupBy: <T>(data: T[], descriptors?: GroupDescriptor[], transformers?: Reducer, originalData?: T[]) => T[] | GroupResult[];
